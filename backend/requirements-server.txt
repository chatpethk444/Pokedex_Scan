# Runtime deps for the FastAPI backend ONLY (no training stack).
# Full stack (torch/timm/...) lives in ../../requirements.txt and must NEVER
# be installed on the server image (2GB+).
fastapi>=0.100
uvicorn>=0.20
onnxruntime>=1.20
Pillow>=10
numpy>=1.24
requests>=2.31
python-multipart>=0.0.6
rapidocr-onnxruntime>=1.3
