# Datasets / captures (GBs) — never push.
pokemon_dataset/
Bb/
outputs/backup_pre_bb/
# Training artifacts — keep the repo light (server needs only best.onnx + classes.txt).
outputs/*.pt
outputs/*.csv
outputs/test_report.txt
outputs/history.csv
outputs/split.json
outputs/fetch_manifest.json
# Toolchains / caches.
**/node_modules/
**/__pycache__/
**/*.pyc
**/*.pyo
app/.expo/
.expo/
# Local-only: agent skills (dev env, not part of the app).
.agents/
# Large experiment model — Docker/Render only needs outputs/best.onnx.
# best.onnx stays tracked on purpose (backend/Dockerfile COPYs it).
outputs/best_nonneon.onnx
